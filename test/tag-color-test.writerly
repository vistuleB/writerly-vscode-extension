|> section
    title=Testing tag name color
    description=Tag name should match |> color

|> header
    level=1

|> navigation
    style=horizontal

|> content-area
    layout=grid

|> sidebar
    position=left

|> footer
    copyright=2024

This file tests that tag names appear in the same color as the |> pipe symbols.
All tag names (section, header, navigation, content-area, sidebar, footer)
should have the same color as their corresponding |> symbols.

|> complex-tag.with_dots:namespace
    test=complex-tag-naming

|> _private
    visibility=hidden

|> data-component
    type=interactive

|> xml:element
    xmlns=namespace

Regular text paragraph for comparison.

|> final-test
    success=true
