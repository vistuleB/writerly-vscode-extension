! This file tests invalid attribute keys that should NOT be recognized as attributes

|> section
    title=valid-attribute
    123invalid=cannot-start-with-number
    good=this-should-still-work

Everything after "123invalid=" should be treated as plain text.

|> another-test
    valid.key=works-fine
    -invalid=cannot-start-with-hyphen
    also.broken=should-be-plain-text

The "-invalid=" line breaks attribute parsing.

|> more-invalid-cases
    working=fine
    .invalid=cannot-start-with-dot
    not.parsed=anymore

Dot at start is invalid, so subsequent lines become plain text.

|> special-characters
    good:namespace=valid
    @invalid=at-symbol-not-allowed
    ignored=plain-text

Special characters like @ are not allowed.

|> numeric-start-cases
    valid_key=ok
    9invalid=number-start-not-allowed
    broken=text

Numbers at start are invalid.

|> mixed-valid-invalid
    first=valid
    second.prop:ns=also-valid
     spaced=space-before-key-invalid
    ignored=plain-text

Space before attribute key makes it invalid.

|> edge-cases
    a=single-letter-ok
    _=single-underscore-ok
    123=just-number-invalid
    $=dollar-symbol-invalid

Testing minimal and special cases.

|> empty-cases
    =empty-key-invalid
    good=value
    should.be=plain-text

Empty key before equals is invalid.

|> valid-examples-for-reference
    standard=attribute
    data-value=hyphen-style
    my_underscore=underscore-style
    namespace:prop=colon-style
    object.property=dot-style
    _private=underscore-start
    complex.nested:namespace=all-valid-chars

These should all work correctly.

Regular paragraph text to separate sections.

|> final-validation
    works.fine:test=valid-complex-key
    1fails=number-start-fails
    everything.after=plain-text
