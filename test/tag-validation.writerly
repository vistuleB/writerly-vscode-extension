|> valid-tag
    title=This is a valid tag

|> another_valid-tag.with:colon
    description=Complex valid tag name

|> _private
    type=Underscore start is valid

|> xml:namespace
    xmlns=Colon start is valid

Empty tag tests (should show errors):

|>
    content=Empty tag should error

|>>
    content=Empty tag with >> should error

|>
    content=Empty tag with spaces should error

Invalid tag name patterns (should show errors):

|> 123invalid
    reason=Cannot start with number

|> -invalid
    reason=Cannot start with hyphen

|> .invalid
    reason=Cannot start with dot

|> @invalid
    reason=Invalid character at symbol

|> #invalid
    reason=Invalid character hash

|> $invalid
    reason=Invalid character dollar

|> invalid tag with spaces
    reason=Spaces not allowed in tag names

|> invalid@character
    reason=At symbol not allowed

|> invalid#hash
    reason=Hash not allowed

|> invalid$dollar
    reason=Dollar not allowed

|> invalid%percent
    reason=Percent not allowed

|> tag*asterisk
    reason=Asterisk not allowed

Valid edge cases:

|> a
    single=Single letter tag is valid

|> A
    caps=Uppercase single letter valid

|> tag123
    numbers=Numbers at end are valid

|> my-complex.tag_name:with-everything
    all=All valid characters combined

Extra content after tag (should show errors with colleague's strict pattern):

|> tag extra content
    error=Should not allow extra content

|> section some text here
    error=Should not allow text after tag

|> valid-tag    trailing spaces are ok
    error=Should not allow non-space content

Valid with trailing spaces (should be OK):

|> tag-with-spaces
    ok=Trailing spaces should be allowed

|> another-tag
    ok=Trailing tab should be allowed

Recovery test:

|> recovery-tag
    success=Should parse normally after errors
