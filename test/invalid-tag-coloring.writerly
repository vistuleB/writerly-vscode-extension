|> valid-tag
    title=This is a valid tag with proper coloring

|> another-valid.tag:namespace
    content=Another valid tag

Testing invalid tags that should keep tag color but show red underline:

|> 123invalid-starts-with-number
    error=Should be colored like valid tags

|> -invalid-starts-with-hyphen
    error=Should be colored like valid tags

|> .invalid-starts-with-dot
    error=Should be colored like valid tags

|> @invalid-special-character
    error=Should be colored like valid tags

|> #invalid-hash-character
    error=Should be colored like valid tags

|> $invalid-dollar-character
    error=Should be colored like valid tags

|> invalid tag with spaces
    error=Should be colored like valid tags

|> tag@with@symbols
    error=Should be colored like valid tags

|> tag#with#hash
    error=Should be colored like valid tags

|> tag$with$dollar
    error=Should be colored like valid tags

Empty tags should also keep pipe color:

|>
    error=Empty tag should keep pipe color

|>>
    error=Empty tag with >> should keep pipe color

|>
    error=Empty tag with spaces should keep pipe color

Extra content after tag should keep coloring:

|> valid-tag extra content here
    error=Should keep tag color despite extra content

|> section some additional text
    error=Should keep tag color despite extra text

Expected behavior:
- All |> symbols should have the same color (keyword.control.wly)
- All tag names (valid or invalid) should have the same color (keyword.control.wly)
- Invalid tags should show red underline errors but maintain their syntax coloring
- No tag should appear in black/default text color
